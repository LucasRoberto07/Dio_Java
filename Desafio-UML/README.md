# Desafio de Projeto UML - Modelando um iPhone

Este projeto é uma representação do desafio de projeto da [Digital Innovation One (DIO)](https://www.dio.me/) sobre a modelagem e implementação em Java das funcionalidades de um iPhone, com base em um diagrama UML.

## 📱 Descrição do Desafio

O objetivo foi modelar e implementar, em Java, as funcionalidades de um iPhone, focando em três papéis principais:

1.  **Reprodutor Musical:** Funções como `tocar()`, `pausar()` e `selecionarMusica()`.
2.  **Aparelho Telefônico:** Funções como `ligar()`, `atender()` e `iniciarCorreioVoz()`.
3.  **Navegador na Internet:** Funções como `exibirPagina()`, `adicionarNovaAba()` e `atualizarPagina()`.

A estrutura do projeto foi baseada em interfaces para representar cada um desses papéis, e uma classe `Iphone` que as implementa.

## Diagrama UML

*(Você pode adicionar uma imagem do seu diagrama UML aqui)*

!UML Diagram

## Folder Structure

The workspace contains two folders by default, where:

- `src`: the folder to maintain sources
- `bin`: a pasta que contém os arquivos `.class` compilados.

## 🛠️ Tecnologias Utilizadas

- **Java:** Linguagem de programação principal utilizada no projeto.
- **UML:** Linguagem de Modelagem Unificada para o design do sistema.
